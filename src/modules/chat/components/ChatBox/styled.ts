import { styled } from 'config/theme'

export const ChatBoxContainer = styled('div', {
  padding: '15px',
  flexGrow: 1,
  gap: '10px',
  display: 'flex',
  flexDirection: 'column',
  maxHeight: '100%',
  overflowY: 'auto',
})
